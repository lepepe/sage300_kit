# frozen_string_literal: true

RSpec.describe Sage300Kit do
  it "has a version number" do
    expect(Sage300Kit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
